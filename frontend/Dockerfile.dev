FROM node:18-alpine

WORKDIR /app

# Copiar package.json y package-lock.json
COPY package*.json ./

# Instalar dependencias
RUN npm ci

# Exponer puerto 3000 para desarrollo con React
EXPOSE 3000

# Comando para ejecutar en modo desarrollo
CMD ["npm", "start"]