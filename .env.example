# Configuración de ejemplo para variables de entorno
# Copia este archivo como .env y configura con tus valores reales

# Base de datos MySQL
DB_HOST=tu_servidor_mysql
DB_USER=tu_usuario_mysql
DB_PASSWORD=tu_password_mysql
DB_NAME=RRHH
DB_PORT=3306

# JWT Secret (genera uno único y seguro)
JWT_SECRET=genera_un_jwt_secret_muy_seguro_aqui

# URLs para CORS (ajusta según tu dominio)
CORS_ORIGIN=http://localhost:3000,https://tu-dominio.com

# URL del API para frontend
REACT_APP_API_URL=http://localhost:3001