# Variables de entorno para Backend
# Copia este archivo a .env y ajusta los valores según tu entorno

# Base de datos
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=tu_password
DB_NAME=RRHH
DB_PORT=3306

# JWT
JWT_SECRET=claveultrasecreta123

# Node
NODE_ENV=development
HOST=0.0.0.0
PORT=3001

# CORS - Lista de orígenes permitidos separados por coma
# Ejemplo local: CORS_ORIGIN="http://localhost:3002,http://127.0.0.1:3002"
# Ejemplo producción: CORS_ORIGIN="http://rrhh.dbconsulting.com.ar,https://rrhh.dbconsulting.com.ar"
CORS_ORIGIN="http://localhost:3002,http://localhost,http://127.0.0.1:3002"
